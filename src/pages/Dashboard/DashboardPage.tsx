const DashboardPage = (): React.JSX.Element => {
    return (
        <main style={{ padding: '48px' }}>
            <h1>Dashboard</h1>

            <p>Dashboard coming in Phase 1.</p>
        </main>
    );
};

export default DashboardPage;
